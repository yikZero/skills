# Roadmap: {{PROJECT_NAME}}

## Now

- Confirm the first target user.
- Define the first useful workflow.
- Select the smallest stack or platform setup that can implement and validate that workflow.
- Keep `{{VALIDATE_COMMAND}}` green.

## Next

- Add product code for the first workflow.
- Add stack-specific lint, typecheck, integration tests, or browser checks.
- Update `PRODUCT.md`, `ARCHITECTURE.md`, and `AGENTS.md` as behavior becomes real.

## Later

- Add deployment, CI, monitoring, and release workflow after local validation is meaningful.
- Extract reusable workflows into skills only after they repeat.

## Explicit Non-Priorities

- Premature plugin marketplaces, multi-tenant architecture, and broad automation.
- Large dependency stacks without a current workflow that needs them.
