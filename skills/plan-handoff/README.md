# Plan Handoff

Audit a codebase in one focus area, then write prioritized, self-contained implementation plans into `plans/` that any executor agent — including cheaper models — can apply with zero extra context.

## Install

```bash
npx yskill --skill plan-handoff
```

## Entry

- [SKILL.md](./SKILL.md)
